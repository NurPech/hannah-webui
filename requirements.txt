grpcio>=1.83.1      # gRPC-Laufzeit (Client) — Untergrenze an die grpcio-tools-Version gekoppelt, die hannah-proto zuletzt zur Stub-Generierung benutzt hat: hannah-proto>=3.9.0's generated *_pb2_grpc.py hard-fails at import below this (see hannah_pb2_grpc.py's version check)
protobuf>=7.35.0    # von den generierten *_pb2.py-Stubs benötigt, kein transitives grpcio-Dependency
flask>=3.0.0        # WebUI
PyYAML>=6.0         # Config-Loading
gunicorn>=21.2.0    # Produktions-WSGI-Server (siehe deploy/hannah-webui.service); Flask-Dev-Server (main.py) bleibt nur für lokale Entwicklung

# Proto schema — published package instead of the git-submodule/local-codegen
# pattern (#60); pins the runtime proto/gRPC-version coupling that used to be
# handled by matching grpcio-tools' version locally
hannah-proto>=4.0.0    # 3.9.0 adds StartVoiceEnrollment/StartVoiceEnrollmentRequest (speaker_enrollment.proto, #52)